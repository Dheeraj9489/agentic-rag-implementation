# === Provider selection ===
# LLM_PROVIDER=openai        # "openai" (default) or "ollama"

# === OpenAI settings ===
OPENAI_API_KEY=sk-your-key-here

# === Ollama settings (only needed when LLM_PROVIDER=ollama or --ollama flag) ===
# OLLAMA_LLM_MODEL=llama3               # any model pulled via `ollama pull`
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text
# OLLAMA_BASE_URL=http://localhost:11434
